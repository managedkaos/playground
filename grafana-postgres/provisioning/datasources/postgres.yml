apiVersion: 1

datasources:
  - name: PostgreSQL
    type: postgres
    access: proxy
    url: postgres:5432
    user: $POSTGRES_USER
    jsonData:
      database: $POSTGRES_DB
      sslmode: disable
    secureJsonData:
      password: $POSTGRES_PASSWORD
    isDefault: true
    editable: true
